<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t ".error_contain" %>
      <%= pluralize object.errors.size, t(".error") %>.
    </div>
    <ul>
      <% object.errors.each do |key, message| %>
        <li><%= key.to_s + " " + message.to_s %></li>
      <% end %>
    </ul>
  </div>
<% end %>
